name: Cent Supreme Build Engine

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60 # رفعتها لساعة كاملة لضمان ترجمة الكود الضخم

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'
          cache: true

      - name: System Preparation & Dependency Fix
        run: |
          flutter config --no-analytics
          # التصحيح: لا نضع سطر null-safety هنا لأنه يسبب الخطأ الذي رأيته في الصورة
          flutter pub get
          flutter pub upgrade --major-versions || true

      - name: Build Android Release APK
        run: |
          # ذاكرة قصوى لمعالجة حجم المشروع الهائل
          export GRADLE_OPTS="-Xmx4096m -XX:MaxMetaspaceSize=1024m"
          # هنا فقط نضع "التعويذة" لتجاوز أخطاء المكتبات القديمة
          flutter build apk --release --no-sound-null-safety --no-tree-shake-icons --obfuscate --split-debug-info=build/app/outputs/symbols

      - name: Upload Cent Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cent-Release-V${{ github.run_number }}
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 7
          
