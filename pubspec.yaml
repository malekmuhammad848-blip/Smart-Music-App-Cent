name: cent
description: Cent Music - Professional Audio Streaming Application
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Audio Engine (Tested and Verified)
  just_audio: 0.9.36
  audio_session: 0.1.18
  rxdart: 0.27.7
  
  # UI & Design
  google_fonts: 6.1.0
  cupertino_icons: 1.0.6
  flutter_spinkit: 5.2.0
  cached_network_image: 3.3.0
  
  # System & Permissions
  path_provider: 2.1.2
  crypto: 3.0.3
  permission_handler: 11.3.1
  
  # YouTube Integration
  youtube_explode_dart: 2.2.2
  
  # State Management
  provider: 6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 3.0.0

flutter:
  uses-material-design: true
  
  # Remove or keep based on actual font file
  # fonts:
  #   - family: Michroma
  #     fonts:
  #       - asset: fonts/Michroma-Regular.ttf
  
  # Add assets if they exist
  # assets:
  #   - assets/
