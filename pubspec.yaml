name: cent_music
description: CENT Music Streaming Platform
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  rive: ^0.11.1
  provider: ^6.0.5
  just_audio: ^0.9.36
  audio_service: ^0.18.12
  path_provider: ^2.1.1
  cached_network_image: ^3.3.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.7
  dio: ^5.3.3
  youtube_explode_dart: ^2.2.2
  # المكتبة الضرورية لتشغيل الفيديو والصوت بدون حظر
  youtube_player_flutter: ^8.1.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
  
